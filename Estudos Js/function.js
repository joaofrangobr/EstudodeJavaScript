function executeName(name, age) { // aqui a função "executeName" é definida, ela recebe dois parâmetros: "name" e "age". Esses parâmetros são usados para passar valores para a função quando ela é chamada.
    console.log("Meu nome é", name, "e tenho", age, "anos"); // dentro da função, usamos console.log para imprimir uma mensagem no console. A mensagem inclui o valor do parâmetro "name" e "age" usando vírgulas para separá-los.
}

executeName("João", "17"); // Aqui temos uma função chamada "executeName" que recebe dois parâmetros: "name" e "age". A função imprime uma mensagem no console usando esses parâmetros. Em seguida, chamamos a função passando os valores "João" e "17".